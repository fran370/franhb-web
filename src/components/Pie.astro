---
import { SOCIAL } from '../consts';
const anio = new Date().getFullYear();
---

<footer class="pie marca">
  <a href={SOCIAL.find((s) => s.nombre === 'Email')?.url}>Email</a>
  <a href="/newsletters/">Newsletter</a>
  <a href={SOCIAL.find((s) => s.nombre === 'LinkedIn')?.url}>LinkedIn</a>
  <a href={SOCIAL.find((s) => s.nombre === 'X')?.url}>X</a>
  <a href="/rss.xml">RSS</a>
  <span class="pie__nota">{anio}</span>
</footer>
